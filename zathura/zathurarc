include atheme

set adjust-open "best-fit"

set adjust-open width

map p search previous
map { navigate previous
map } navigate next

unmap K navigate previous
unmap J navigate next

set window-title-basename "true"
set selection-clipboard "clipboard"
